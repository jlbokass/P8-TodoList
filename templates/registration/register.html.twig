{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <div class="container" style="margin-top: 100px; margin-bottom: 100px">
        <h1 class="text-center">Inscription</h1>
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-sm-12">
                        {% include 'registration/registrationForm.html.twig' %}
                        <button type="submit" class="btn btn-md btn-success">S'inscrire</button>
                        {{ form_end(registrationForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}